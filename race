Agent::Race
ItemPositon target[];
int currentIndex;
 
Init()
{
	pre (loc == init)
	eff 
	{
	    target = getInput(); 
	    currentIndex = 0; 
	    loc = pick;
	}
}
update()
{
	pre (currentIndex < targets.size() 